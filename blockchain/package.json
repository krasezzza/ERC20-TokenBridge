{
  "name": "erctwenty-tokenbridge-bc",
  "version": "1.3.0",
  "scripts": {
    "test": "npx hardhat test",
    "start": "npx hardhat node",
    "compile": "npx hardhat compile",
    "coverage": "npx hardhat coverage",
    "deploy-local": "npx hardhat compile && npx hardhat deploy-local",
    "deploy-goerli": "npx hardhat compile && npx hardhat deploy-goerli --network goerli",
    "deploy-sepolia": "npx hardhat compile && npx hardhat deploy-sepolia --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  }
}
